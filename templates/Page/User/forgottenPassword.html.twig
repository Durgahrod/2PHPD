{% extends 'Layout/base.html.twig' %}

{% block body %}
    {{ form_start(forgotten_password_user_type) }}
    {{ form_rest(forgotten_password_user_type) }}
    <input type="submit">
    {{ form_end(forgotten_password_user_type) }}
{% endblock body %}
