{% extends 'Layout/base.html.twig' %}

{% block body %}
    {{ form_start(reset_user_type) }}
    {{ form_rest(reset_user_type) }}
    <input type="submit">
    {{ form_end(reset_user_type) }}
{% endblock body %}